# Web framework
Flask>=3.0
Flask-Login>=0.6
Flask-SQLAlchemy>=3.1   # user/account store (app DB, separate from analytics)
Flask-WTF>=1.2          # login form + CSRF protection
gunicorn>=22.0          # production WSGI server (deploy later)

# Interactive dashboards (mounted on Flask)
dash>=2.17
plotly>=5.22

# Data layer
pandas>=2.2,<3   # unpinned <3 let CI resolve a newer major than local dev pins, causing a real hittrax crash (2026-08-19)
SQLAlchemy>=2.0
PyMySQL>=1.1

# Config / env
python-dotenv>=1.0

# Data ingestion (Trackman SFTP / HitTrax FTPS loaders)
paramiko>=3

# Roster media scrape (scripts/scrape_roster_media.py)
beautifulsoup4>=4.12

# Reports (HTML -> PDF)
# PINNED, not >=: playwright stamps its browser directory with a build number
# tied to the package version (1.61.0 -> chromium_headless_shell-1228). A float
# upgrade leaves the package looking for a build the installed browsers aren't,
# and every report 500s with "Executable doesn't exist ... Looks like Playwright
# was just installed or updated". Bump this and the browser install together.
playwright==1.61.0   # headless Chromium for PDF rendering
kaleido>=0.2         # Plotly static image export
matplotlib>=3.8      # one-pager chart rendering (Agg backend, no browser)
Pillow>=10.0         # report logo/image assets (also scripts/make_lion_white.py)
